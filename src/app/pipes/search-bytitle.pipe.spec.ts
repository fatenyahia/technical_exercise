import { SearchBytitlePipe } from './search-bytitle.pipe';

describe('SearchBytitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBytitlePipe();
    expect(pipe).toBeTruthy();
  });
});
